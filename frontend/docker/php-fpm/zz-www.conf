[www]
clear_env = no

; basic worker config (tune later)
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8

; log to stderr/stdout (docker friendly)
catch_workers_output = yes
access.log = /proc/self/fd/2
