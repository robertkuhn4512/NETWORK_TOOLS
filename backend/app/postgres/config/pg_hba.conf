# TYPE   DATABASE  USER  ADDRESS           METHOD

# Local socket connections inside the container
local    all       all                     scram-sha-256

# Localhost (inside container)
hostssl  all       all   127.0.0.1/32      scram-sha-256
hostssl  all       all   ::1/128           scram-sha-256

# Docker network (REPLACE subnet with your actual docker network subnet)
hostssl  all       all   172.18.0.0/16     scram-sha-256


# TEMP TEST: allow all IPv4/IPv6 clients, require password auth
host    all    all    0.0.0.0/0    scram-sha-256
host    all    all    ::/0         scram-sha-256